# BMW Quotation — คู่มือสำหรับ Claude Code

## บริบท
เครื่องมือคำนวณ/เปรียบเทียบยอดผ่อนรถ BMW สำหรับพนักงานขาย **ใช้คนเดียว** (เจ้าของ repo)
Deploy อัตโนมัติที่ Vercel เมื่อ push ขึ้น `main` → https://bmw-quotation.vercel.app

Stack: React + Vite + Tailwind + Firebase
โครงสร้างหลัก: `src/App.jsx` (monolith), `src/utils/financeEngine.js` (เครื่องคำนวณ), `verify.mjs` (ชุดทดสอบ)

**ตอบเป็นภาษาไทย**

---

## กฎเหล็ก — ห้ามฝ่าฝืน

1. **ห้ามแก้ `src/utils/financeEngine.js`** ไฟล์นี้ผ่านการ verify แล้ว 22/22 จุด
   ถ้าคิดว่าจำเป็นต้องแก้ ให้หยุดแล้วถามเจ้าของก่อนเสมอ

2. **ก่อน `git push` ทุกครั้ง ต้องรัน `node verify.mjs`**
   ต้องได้ครบ 22/22 — ถ้าไม่ผ่าน **ห้าม push เด็ดขาด**

3. **ห้าม refactor / จัดระเบียบโค้ด / แยก component โดยไม่ได้สั่ง**
   `App.jsx` ยาว 2,000+ บรรทัดโดยตั้งใจ ปล่อยไว้แบบนั้น — โค้ดสะอาดไม่ได้ทำให้ขายรถได้เพิ่ม

4. **หนึ่ง session ทำหนึ่งเป้าหมาย** เสร็จแล้วหยุด อย่าเสนอทำอย่างอื่นต่อ

5. **ห้ามสร้างโปรเจค/repo ใหม่** ไม่ว่าจะรู้สึกว่าของเดิมรกแค่ไหน

6. **ถามก่อนแก้เสมอ ถ้าการแก้นั้นกระทบตัวเลขที่ลูกค้าเห็น**

---

## ข้อตกลงทางธุรกิจที่ล็อกแล้ว (อย่าเปลี่ยนเอง)

- **BSI รวมอยู่ในราคาเสมอ** — ก่อนเรียก `calculate()` ต้องผ่าน wrapper `calcWithBsi`
  ที่ merge `bsi` เข้า `carPrice` แล้วส่ง `bsi: 0` เข้าไปแทน
- **ฐานคิดเงินดาวน์ / Deposit / Balloon = ราคารวม BSI**
- **ฐานคิด GFV (โหมด FC) = ราคา Retail ที่ไม่รวม BSI**
- **Add. Interest ส่วนที่เกิน 0.5% ต้องหาร 2 ก่อนบวกเข้า Real SF Rate**
- ปัดเศษ: จำนวนเงิน 2 ตำแหน่ง · อัตราดอกเบี้ย 4 ตำแหน่ง

---

## โหมดที่รองรับ
`HP` (Flat) · `HP-BL` (Balloon) · `FC` (Freedom Choice) · `FL` (Leasing) · `FL-BL` (Leasing Balloon)

---

## การทดสอบ
`verify.mjs` มี 22 จุดตรวจ:
- 17 จุดจาก Test Case ตายตัว 5 เคส (ที่มา: `Quick_Quote_Project_Spec_v1_3.md` ภาคผนวก A.5)
- 5 จุดจาก BSI-merge invariant — กรอก "ราคารวม BSI + BSI 0" กับ "ราคาเปล่า + BSI แยก" ต้องได้ค่างวดเท่ากันทุกโหมด

เพิ่มเคสใหม่ได้ แต่**ห้ามลบหรือแก้เคสเดิม**

> verify.mjs ครอบคลุมเฉพาะการคำนวณ ไม่ครอบคลุม UI
> งานที่แตะหน้าจอต้องทดสอบด้วยตาเสมอ — 22/22 ไม่ได้แปลว่าหน้าจอไม่พัง

---

## Workflow มาตรฐาน
```
npm run dev          # รันดูในเครื่องก่อนเสมอ
node verify.mjs      # ต้อง 22/22
git add -A && git commit -m "..." && git push origin main
```
